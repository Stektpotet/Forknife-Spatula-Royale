
CREATE TABLE IF NOT EXISTS `recipe`
(
    _ID 	        int auto_increment 	NOT NULL,
    title           varchar(250)        NOT NULL,
    cookTime        varchar(15)         NOT NULL,
    description     varchar(2000)        NOT NULL,
    instructions    varchar(5000)        NOT NULL,

	PRIMARY KEY (_ID)
);

CREATE TABLE IF NOT EXISTS `recipeContent`
(
	recipeId 		int 	NOT NULL,
	ingredientId	int 	NOT NULL,

	PRIMARY KEY(recipeId, ingredientId),

	FOREIGN KEY(recipeId)
		REFERENCES recipe(_ID)
		ON UPDATE RESTRICT
		ON DELETE CASCADE,

	FOREIGN KEY(ingredientId)
		REFERENCES ingredient(_ID)
		ON UPDATE RESTRICT
		ON DELETE CASCADE
);


INSERT INTO ingredient (name) VALUES ("New Eggs");
INSERT INTO ingredient (name) VALUES ("New Chicken ");
INSERT INTO ingredient (name) VALUES ("New Minced ");
INSERT INTO ingredient (name) VALUES ("New Garlic");
INSERT INTO ingredient (name) VALUES ("New Onions");
INSERT INTO ingredient (name) VALUES ("New Corn");
INSERT INTO ingredient (name) VALUES ("New Asparagus");


